#include<stdio.h>
struct poly
{
	float exp;
	float coeff;
	strct poly *next;
}
type def struct poly node;
node *phead,*qhead;
void create(node *head)
{
	int n,k;
	node *p;
	printf("ENTER THE NO OF ELEMENT");
	scanf("%d",&n);
	for(k=0;k<n;k++)
	{
		if(k=0)
		{
			head=(node*)malloc(sizeof(node))
		}
		
	}
}
int main()
{
		
} 
